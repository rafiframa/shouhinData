<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Database</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <header
        style="background: #fff; padding: 1rem 2rem; border-bottom: 1px solid #eee; display: flex; justify-content: space-around; align-items: center;">
        <div style="display: flex; align-items: center;">
            <img src="/images/logo.png" alt="Company Logo" height="25">
            <h1 style="color: #333; margin-left: 10px;">商品データベース</h1>
        </div>

        <% if (typeof isAuthenticated !=='undefined' && isAuthenticated) { %>
            <div style="display: flex; align-items: center; gap: 1rem;">
                <% if (user && user.profile_picture) { %>
                    <img src="<%= user.profile_picture %>" alt="Profile"
                        style="width: 32px; height: 32px; border-radius: 50%;">
                    <% } %>
                        <% if (user && user.name) { %>
                            <span style="color: #666;">
                                <%= user.email %>
                            </span>
                            <% } %>
                                <% if (user && user.role==='admin' ) { %>
                                    <span
                                        style="background: #28a745; color: white; padding: 2px 8px; border-radius: 12px; font-size: 12px;">管理者</span>
                                    <% } %>
                                        <a href="/logout" style="color: #dc3545; text-decoration: none;">ログアウト</a>
            </div>
            <% } %>
    </header>

    <main class="container">